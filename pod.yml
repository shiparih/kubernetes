apiVersion: v1
kind: Pod
metadata:
  name: nodeapp
  labels:
    app: nodeapp
spec:
  containers:
    - name: nodeapp
      image: shiparih/cheers2019:latest
      ports:
        - containerPort: 8080
